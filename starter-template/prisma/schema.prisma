generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Plants {
    id        Int      @id @default(autoincrement())
    name      String   @unique
    genus     String   @unique
    description String  
    isOutdoor   Boolean    
}

